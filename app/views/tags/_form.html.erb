
<%= form.simple_fields_for :tags, :html => { :class => 'form-horizontal' } do |tag_form| %>

	  <%= tag_form.input :name, label: 'Tag',required: false  %>

  <% unless tag_form.object.nil? || tag_form.object.new_record? %>
<%= tag_form.input :_destroy, label: 'Remove:',required: false,:as => :boolean  %>
	<% end %>
<% end %>